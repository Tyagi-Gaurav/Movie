spring:
  active: local

api-gateway:
  host: ${API_GATEWAY_HOST:localhost}
  port: ${API_GATEWAY_PORT:8080}
  contentUploadContextPath: /content/upload
  userContextPath: /user

movie-app:
  host: localhost
  port: 9090

admin-credentials:
  userName: admin
  password: ${ADMIN_PASSWORD:admin123}