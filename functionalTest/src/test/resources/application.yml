spring:
  active: local

api-gateway:
  host: ${API_GATEWAY_HOST:api.movie.com}
  port: ${API_GATEWAY_PORT:8080}
  contextPath: /content/upload/

movie-app:
  host: localhost
  port: 9090

user-app:
  host: ${USER_APP_HOST:localhost}
  port: ${USER_APP_PORT:7070}
  mgtPort: ${USER_MGT_PORT:7071}

admin-credentials:
  userName: admin
  password: ${ADMIN_PASSWORD:admin123}