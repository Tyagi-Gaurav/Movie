spring:
  active: local

movie-app:
  host: ${MOVIE_APP_HOST:localhost}
  port: ${MOVIE_APP_PORT:9090}
  mgtPort: ${MOVIE_MGT_PORT:9091}

user-app:
  host: ${USER_APP_HOST:localhost}
  port: ${USER_APP_PORT:7070}
  mgtPort: ${USER_MGT_PORT:7071}

admin-credentials:
  userName: admin
  password: ${ADMIN_PASSWORD:admin123}