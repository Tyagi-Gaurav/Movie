spring:
  active: local

movie-app:
  host: ${MOVIE_APP_HOST:api.movie.com}
  port: ${MOVIE_APP_PORT:8080}
  contextPath: /content/upload/
  mgtPort: ${MOVIE_MGT_PORT:8080}

user-app:
  host: ${USER_APP_HOST:localhost}
  port: ${USER_APP_PORT:7070}
  mgtPort: ${USER_MGT_PORT:7071}

admin-credentials:
  userName: admin
  password: ${ADMIN_PASSWORD:admin123}